import { storiesOf } from '@storybook/react';
import { MonitorSection } from './MonitorSection';

storiesOf('Ailalia/Templates/Monitor/MonitorSection', module).add(
  'MonitorSection',
  () => {
    return <MonitorSection />;
  },
);
